import "./Checkout.css"
// TODO : include stripe and 


const Checkout = (props) =>
{

    // add UI elements to allow user to
    // enter payment details
    return(
        <div className="Checkout">
            Checkout Works
        </div>
    )
}

export default Checkout;